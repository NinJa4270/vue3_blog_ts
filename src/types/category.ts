export interface IdialogConfig {
  visible: boolean;
  title: string;
  id: number;
}
export interface INavForm {
  name: string;
  pid: number;
  path: string;
  role_id: number;
}
export interface ISelectItem {
  value:number;
  label:string;
}

